{
  "name": "pms-fullstack",
  "version": "1.0.0",
  "description": "Project Management System",
  "main": "index.js",
  "scripts": {
    "start": "npm start --prefix backend",
    "build": "npm ci --prefix frontend --no-audit --no-fund --production=false && npm run build --prefix frontend && npm ci --prefix backend --no-audit --no-fund --production=false && npm run generate --prefix backend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}